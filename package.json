{
	"name": "inline-svg-preview",
	"displayName": "Inline SVG Preview",
	"description": "Preview svg code inline.",
	"repository": {
		"url": "https://github.com/Mohamed-Eltelb/inline-svg-preview"
	},
	"icon": "image/icon.png",
	"publisher": "Mohamed-Eltelb",
	"author": "Mohamed Eltelb",
	"version": "0.0.8",
	"engines": {
		"vscode": "^1.67.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"onStartupFinished"
	],
	"main": "./out/extension.js",
	"contributes": {
		"commands": [
			{
				"command": "spic.gallery",
				"title": "SVG Gallery"
			}
		],
		"configuration": [
			{
				"title": "SvgPreviewInCode",
				"properties": {
					"spic.include": {
						"type": [
							"array",
							"null"
						],
						"default": null
					},
					"spic.exclude": {
						"type": [
							"array",
							"null"
						],
						"default": null
					},
					"spic.currentColor": {
						"type": "string",
						"default": "auto",
						"markdownDescription": "Color used for `currentColor` in SVG previews. `auto` picks a light color on dark themes and a dark color on light themes. Any CSS color works (e.g. `#ffffff`, `tomato`). Leave empty to disable (renders black, like a plain image)."
					},
					"spic.applyColorToFill": {
						"type": "boolean",
						"default": true,
						"markdownDescription": "Also use `#spic.currentColor#` as the default fill for shapes with no `fill` set (they otherwise render black). SVGs that set their own `fill` or `color` on the root element are left unchanged."
					}
				}
			}
		]
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"watch": "tsc -watch -p ./",
		"build-page": "esbuild ./src/page/gallery/main.tsx --sourcemap --outdir=out/page/gallery --bundle",
		"compile": "tsc -p ./ & esbuild ./src/page/gallery/main.tsx --outdir=out/page/gallery --bundle",
		"pretest": "npm run compile && npm run lint",
		"lint": "eslint src --ext ts",
		"package": "vsce package",
		"publish": "vsce publish",
		"test": "node ./out/test/runTest.js"
	},
	"devDependencies": {
		"@types/glob": "^7.2.0",
		"@types/mocha": "^9.1.1",
		"@types/node": "14.x",
		"@types/react-dom": "^18.0.5",
		"@types/styled-components": "^5.1.25",
		"@types/vscode": "^1.67.0",
		"@types/vscode-webview": "^1.57.0",
		"@typescript-eslint/eslint-plugin": "^5.21.0",
		"@typescript-eslint/parser": "^5.21.0",
		"@vscode/test-electron": "^2.1.3",
		"eslint": "^8.14.0",
		"mocha": "^9.2.2",
		"typescript": "^4.6.4",
		"vsce": "^2.9.1"
	},
	"dependencies": {
		"esbuild": "^0.14.42",
		"fast-xml-parser": "^4.0.7",
		"glob": "^8.0.3",
		"react": "^18.1.0",
		"react-dom": "^18.1.0",
		"styled-components": "^5.3.5"
	}
}
